<%= form_for(@slider) do |f| %>
  <% if @slider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slider.errors.count, "error") %> prohibited this slider from being saved:</h2>

      <ul>
      <% @slider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

<div class="field">
    <%= f.label :description %><br />
    <%= f.cktext_area :description, :toolbar=>'slim', :width=>'100%', :height=>'200px', :skin => "v2" %>
  </div>

  <div class="field">
    <%= f.label :image_tag %><br />
    <%= f.select(:image_tag, options_for_select([['BRC-300', 1], ['EVI-HD1', 2], ['EVI-D70', 3]])) %>
  </div>  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= debug params %>
